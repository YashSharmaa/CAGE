# CAGE Docker Compose Environment Variables
# Copy this to .env and fill in values

# Security tokens (generate with: openssl rand -base64 32)
CAGE__SECURITY__JWT_SECRET=change-me-generate-secure-secret-min-32-chars
CAGE__SECURITY__ADMIN_TOKEN=change-me-generate-admin-token

# Optional: Customize resource limits
# CAGE__DEFAULT_LIMITS__MAX_MEMORY_MB=1024
# CAGE__DEFAULT_LIMITS__MAX_CPUS=1.0
# CAGE__DEFAULT_LIMITS__MAX_PIDS=100

# Optional: Enable debug logging
# CAGE__LOG_LEVEL=debug
