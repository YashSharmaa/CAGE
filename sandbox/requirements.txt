# CAGE Sandbox Python Dependencies
# Curated set of commonly needed packages for data science and general coding tasks
# Mirrors capabilities of OpenAI Code Interpreter environment

# Core data science
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Data processing
openpyxl>=3.1.0      # Excel files
xlrd>=2.0.0          # Legacy Excel
python-docx>=1.0.0   # Word documents
PyPDF2>=3.0.0        # PDF processing
Pillow>=10.0.0       # Image processing
beautifulsoup4>=4.12.0  # HTML parsing
lxml>=4.9.0          # XML processing

# File formats
pyyaml>=6.0.0
toml>=0.10.0
python-dotenv>=1.0.0

# Math and statistics
sympy>=1.12          # Symbolic math
statsmodels>=0.14.0  # Statistical models
scikit-learn>=1.3.0  # Machine learning

# Utilities
requests>=2.31.0     # HTTP client (will fail without network but useful for allowed cases)
python-dateutil>=2.8.0
pytz>=2023.3
tqdm>=4.66.0         # Progress bars
tabulate>=0.9.0      # Table formatting
jinja2>=3.1.0        # Templating

# JSON/Data handling
orjson>=3.9.0        # Fast JSON
jsonschema>=4.19.0   # JSON validation

# Text processing
regex>=2023.10.0
chardet>=5.2.0       # Character encoding detection

# Compression
python-magic>=0.4.27 # File type detection

# Jupyter for persistent interpreter mode
ipykernel>=6.25.0
ipython>=8.15.0

# Note: Some packages like tensorflow, torch are excluded
# due to size. They can be added in specialized images if needed.
